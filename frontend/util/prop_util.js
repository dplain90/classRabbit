export const setStateForDifProps = (oldProp, newProp, keys) => {
  for (const i = 0; i < Object.keys(keys).length; i++) {
    let key = Object.keys(keys)[i];
    if(oldProp[key] !== newProp[key]) {
      this.setState({}
    }
  }
  Object.keys.forEach({


  });

}
